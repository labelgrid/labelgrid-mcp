node_modules/
dist/
.env
.env.contract.local
*.local.mjs
*.log
.DS_Store
tmp/
.impeccable/
mcpb/build/
mcpb/deps/
labelgrid.mcpb
# The API document is resolved at run time and must never be committed here.
# No leading slash, so this matches at any depth.
openapi.json
